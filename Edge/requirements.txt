numpy>=1.19.0
opencv-python-headless>=4.5.0
PyYAML>=5.4.0
aiohttp>=3.13.0
PyQt5
watchdog>=3.0.0
python-dotenv>=1.0.0

# -----------------------------------------------------------
# Zenoh P2P — install from Eclipse's own package index:
#   pip install zenoh msgpack \
#     -i https://download.eclipse.org/zenoh/zenoh-python/releases/
# -----------------------------------------------------------
zenoh>=1.3.0
msgpack>=1.1.0

# WS Client for MonitorClient (daemon thread → synchronous)
websocket-client>=1.6.0

# Jetson metrics (Jetson only)
# jetson-stats>=4.0.0
