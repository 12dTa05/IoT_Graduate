# =============================================================
# Edge/requirements.txt — Python dependencies for Edge node
#
# Install:  pip3 install -r requirements.txt
# Platform: Jetson Orin (aarch64), JetPack 6.x, Ubuntu 22.04
# =============================================================

# --- Core ---
numpy>=1.19.0
opencv-python-headless>=4.5.0
PyYAML>=5.4.0
python-dotenv>=1.0.0
msgpack>=1.0.0
psutil>=5.9.0

# --- Networking / Messaging ---
websocket-client>=1.6.0          # MonitorClient WS to Dashboard
aiohttp>=3.9.0                   # PeerOrchestrator REST fallback
aiofiles>=23.0.0                 # violation_store async file writes
eclipse-zenoh>=1.0.0             # P2P peer orchestration

# --- Dynamic Camera Config ---
watchdog>=3.0.0                  # inotify watcher for cameras.yml

# --- REST API (CameraManager) ---
fastapi>=0.100.0
uvicorn>=0.20.0

# --- Jetson Hardware Metrics ---
jetson-stats>=4.0.0              # jtop: GPU/CPU/RAM/Temp/Power
